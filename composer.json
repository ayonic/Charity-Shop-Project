{
    "name": "charity-shop/payment-system",
    "description": "Payment system for charity shop",
    "type": "project",
    "require": {
        "php": ">=7.4",
        "stripe/stripe-php": "^10.0",
        "monolog/monolog": "^2.0"
    },
    "autoload": {
        "psr-4": {
            "CharityShop\\": "includes/"
        }
    },
    "config": {
        "optimize-autoloader": true
    }
}